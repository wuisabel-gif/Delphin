# Summary

What does this change and why?

## Checklist
- [ ] `cargo fmt --all --check` passes
- [ ] `cargo clippy --all-targets -- -D warnings` passes
- [ ] `cargo test --all` passes
- [ ] Default path stays local-first / zero-setup (any network or model use is optional with a fallback)
- [ ] Docs updated if behavior or flags changed

## Notes
Anything reviewers should know — trade-offs, follow-ups, manual testing done.
